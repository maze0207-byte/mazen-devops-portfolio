version: "3.9"

services:
  app:
    image: node:20-alpine
    container_name: dev-portfolio
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev"