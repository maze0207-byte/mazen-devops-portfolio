apiVersion: v1
kind: Service
metadata:
  name: terminal-server
  namespace: portfolio
  labels:
    app: terminal-server
spec:
  type: NodePort
  selector:
    app: terminal-server
  ports:
    - name: ws
      port: 4000
      targetPort: ws
      nodePort: 30400
      protocol: TCP
