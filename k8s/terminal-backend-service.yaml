apiVersion: v1
kind: Service
metadata:
  name: terminal-backend
  namespace: portfolio
  labels:
    app: terminal-backend
spec:
  type: NodePort
  selector:
    app: terminal-backend
  ports:
    - name: ws
      port: 30500
      targetPort: ws
      nodePort: 30500
      protocol: TCP
