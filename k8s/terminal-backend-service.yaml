apiVersion: v1
kind: Service
metadata:
  name: terminal-backend
  namespace: portfolio
  labels:
    app: terminal-backend
spec:
  type: NodePort
  selector:
    app: terminal-backend
  ports:
    - name: ws
      port: 30001
      targetPort: 4000
      nodePort: 30001
      protocol: TCP
